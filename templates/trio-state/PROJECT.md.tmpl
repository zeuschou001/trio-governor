---
name: "{{name}}"
type: "{{type}}"
created_at: "{{created_at}}"
description: "{{description}}"
---

# Project

此文件由 `/trio:dev` 在首次运行的 `/office-hours` 阶段写入 frontmatter，之后为只写一次。
要补充信息请在文件末尾追加 `## Addendum: <标题>` 二级标题。
