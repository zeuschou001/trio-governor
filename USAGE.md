# 使用

## 主入口 `/trio`

日常使用时,优先直接输入:

```text
/trio <你的需求>
```

`trio` 会先做三件事:

1. 判断当前任务更适合 `quick` 还是 `dev`
2. 仅在必要时补问少量信息
3. 在上次任务未完成时要求你决定继续还是重开

## 它什么时候会判成 `quick`

适合这些场景:

- 改动范围小
- 风险明确且较低
- 不需要额外架构决策
- 典型例子:局部 bug 修复、配置修改、样式或文案调整

## 它什么时候会判成 `dev`

出现以下任一情况,就应该走完整流程:

- 新功能
- 跨模块修改
- 数据结构、接口或核心流程变更
- 需要评审关键方案
- 你自己也不确定风险边界

若你明确知道必须强制完整流程,兼容入口仍可带两个额外开关:

- `--with-ceo`:显式要求更重的评审路径
- `--minimal`:减少外部依赖,但不改变完整流程的治理目标

## 你会看到的交互

你只需要理解这些信号:

- 当前模式:`quick` 或 `dev`
- 当前风险:可以直接做,还是需要先补确认
- 当前选择:继续,还是重开
- 当前建议:是否应该回到完整评审

## 兼容入口

仓库里仍保留显式 `dev` / `quick` 入口,适合这两类场景:

- 你要强制完整流程,不想让 `trio` 自动判断
- 你在调试旧工作流,需要保留原有入口面

## 质量要求

- `quick` 不是降低质量要求
- `quick` 只是缩短治理路径
- 风险升高时,`trio` 会把任务拉回 `dev`
- 首次或明显有误用风险的 quick 可能会先要求一次轻量风险确认

## 最小留痕

`.trio/` 只保留治理所需的最小痕迹,例如:

- 当前工作模式
- 关键决策
- 必要的上下文记录
- 未完成任务的恢复线索

## 内部调试接口

仓库根目录下的 `./trio` 是开发者内部工具。以下命令仅供安装、调试和实现验证使用,普通使用者无需直接操作:

```bash
./trio session ...
./trio runtime ...
./trio bridge ...
```

## 本地开发与 CI

```bash
./scripts/measure-tokens.sh
./scripts/ci.sh
```

`measure-tokens` 只做计量统计,不作为失败门禁。
